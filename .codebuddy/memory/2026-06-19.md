# 2026-06-19

## 对话期间 Tab 键禁用按钮焦点
- 问题：对话时按 Tab 键会轮流高亮上方栏三个按钮（Godot 内置 UI 焦点导航行为）
- 修复：在 `top_bar.gd` 添加 `_process()`，对话激活时将三个按钮 `focus_mode` 设为 `FOCUS_NONE`，对话结束后恢复 `FOCUS_ALL`；同时在 `_input` 中拦截 `InputEventKey`

## 莫罗对话后黑屏转场移出
- 问题：莫罗对话结束后未移出场景，仍站在 KARL_POSITION；且移除方式不一致
- 修复：改用 `_start_scene_transition()` 统一流程，黑屏淡出→移出→黑屏淡入。莫罗不在 `_next_npc_map` 中，所以不会有下一个 NPC 登场

## 结局字幕先黑屏再显示
- 修改 `Player2._play_epilogue()`：先在 1.5 秒内淡入黑屏，再在黑底上显示白色结局字幕（右侧 2/3 区域），去掉结尾的二次黑屏叠加（已无必要）

## 解决结局黑屏图层被遮挡
- fade/bg/scroll 设置 `z_index` 99-101，`set_anchors_preset()` 替代直接赋值

## 结局字幕结束后显示返回按钮
- 所有结局行播完后，在屏幕中心下方 1/3 处显示"返回主菜单"按钮，点击跳转 MainMenu

## 修复莫罗后读档无法触发结局
- `_show_switch_dialogue()`（Player1 走近 Player2）新增 `moro_finale_pending` 检查
- `moro_finale_pending` 清除时机从终局独白前移至 `_play_epilogue()` 开头，确保读档后标志仍有效

## Player2 头顶引导感叹号
- 生成黄色感叹号 `assets/textures/exclamation_mark.png`
- Player2 `_ready()` 创建 `Sprite3D`，位置 Y+2.2，billboard 始终面向摄像机
- `_update_guide_mark()` 检查 `monologue_pending && !_is_controlled` 控制显示
- 迪特尔 P1 完成后出现，Player1 与 Player2 对话后消失

## 对话左上角角色名改为中文
- `Npc.gd` `_update_name_labels()`：Player1 → "弗莱"，Player2 → "杜瓦尔"
