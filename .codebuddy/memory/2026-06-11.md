# 2026-06-11

## 代码重构：Player/Player2 重复代码提取
- 创建了 `scripts/BasePlayer.gd` 基类，提取了 Player.gd 和 Player2.gd 中 100% 相同的代码
- Player.gd 从 615 行缩减到 334 行（-46%）
- Player2.gd 从 1041 行缩减到 731 行（-30%）
- 新增 BasePlayer.gd 374 行，总计约 217 行减少（13%）

### 提取到基类的共享代码：
- @export 参数（move_speed, sprint_speed, UI路径等）
- 技能/背包/日志数据（_skill_xp, _skill_level, inventory, journal）
- 点击指示器（_create_click_indicator, _align_ring_to_normal, _clear_click_target）
- 墙壁透明度系统（_init/update_wall_transparency, _aabb_contains）
- 动画工具（_find_ap_recursive, _add_idle_animation, _play_anim_by_keys）
- 交互管理（register/unregister_interactable, _update_nearest_prompt）
- 数据接口（get_skill_xp/level, add_item/journal/skill_xp）
- 工具方法（_ensure_action, _is_any_ui_open, _node_in_group, _snap_to_ground, _tint_model_recursive）

### add_item 差异处理：
- BasePlayer 定义 `_get_item_log_prefix()` 虚拟方法（默认返回 "[背包]"）
- Player2 覆盖返回 "[P2背包]"

### 保守原则：
- 只移动 100% 相同的代码，任何有细微差异的方法（如 _color_model, _center_camera_on_player, _update_camera_basis_cache, _mouse_raycast_full, _update_animation）保留在子类
