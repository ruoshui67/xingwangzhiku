# 2026-06-15

- 将 NPC 对话字体大小从 11 改为 21，修改 `scenes/Main.tscn` 中 `DialogueText` (RichTextLabel) 节点的 `theme_override_font_sizes/normal_font_size` 属性（RichTextLabel 需用 `normal_font_size` 而非 `font_size`）。
- 完成迪特尔 NPC 的完整对话系统：两轮选择（第一轮 5 选项含 2 骰子挑战，第二轮 5 选项含 4 骰子挑战）+ 终局内心独白自动收尾。
- 新增"对话完成后二次交互"机制：在 Npc.gd 中添加 `post_complete_text` 导出变量和完成标志。迪特尔设为"迪特尔恶狠狠的盯着你，一言不发。"
- 新增 Player2 独立对话系统：Npc.gd 中新增 p2_* 导出变量组、_is_player2_interacting()（最近玩家检测）、_swap_to_p2()/_restore_p1()（数据备份/切换）、独立的 _p1/_p2_dialogue_ever_completed 标志。迪特尔配置了完整 P2 对话（杜瓦尔线）：开场 3 行 → 3 选项 → 核心信息释放 → 2 选项 → 收束终局。Player1 和 Player2 对话完全独立，互不影响。
- 完成莫罗 NPC 的完整 P2 对话（杜瓦尔线，5 轮 + 尾声）：开场 4 行 → 4 选项 → 妻子病情 4 行 + 4 选项 → 杜邦轮 4 行 + 3 选项（已修复为完整原文）→ 纸笔陈述 + "继续" → 最后一句话 + "…" → scene_end（审讯室空寂 + 弗莱尾声独白）。莫罗坐标从 (10,-4) 移至 (3,6) 房间内。莫罗 P1 对话待填充。
