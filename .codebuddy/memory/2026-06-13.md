# 2026-06-13

## 主菜单创建
- 创建 `scenes/MainMenu.tscn`：纯黑背景 (ColorRect) + BgImage (TextureRect，可替换)
- 创建 `scripts/MainMenu.gd`：处理新游戏/继续游戏/设置/退出游戏四个按钮
- 四个按钮位于左下角 (VBoxContainer, offset_left=80, offset_top=520)
- 设置面板内含音量/分辨率/显示模式 + 返回按钮
- 修改 `project.godot` 入口场景指向 MainMenu.tscn
- PauseManager 新增"返回主菜单"按钮 (在退出游戏按钮上方)

## 存档系统
- 创建 `scripts/SaveSystem.gd` autoload (project.godot 已注册)
- 存档路径: `user://save_game.cfg` (ConfigFile 格式)
- 存档内容: Player1/Player2 位置+背包+技能+日志+控制权 + NPC对话进度 + AI推理结果
- 自动存档: 每30秒检测 _dirty 标记自动写入
- mark_dirty() 触发点: add_item / add_journal / add_skill_xp (Player+Player2) + _advance_dialogue (Npc)
- 返回主菜单时自动存档 (PauseManager)
## 部署
- Godot HTML5 导出到 `build/web/`，清理后到 `build/deploy/`
- **CloudBase**（主）: `webgame-d5gvpmllx933a460a` → `https://webgame-d5gvpmllx933a460a-1443017001.tcloudbaseapp.com/`
- **EdgeOne**（备）: `dpkftmxdz0gm`
- 更新: 导出 → `build/deploy/` → uploadFiles 到 CloudBase
