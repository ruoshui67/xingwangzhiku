# 2026-06-10 工作记录

## 角色切换系统完善（双向换人 + 位置交换 + 确认对话）

### 双向换人流程
- **换入 Player2**：走近→F → "需要休息吗？"确认框 → [需要] → 交换位置 + 切换控制
- **换回 Player1**：Player2被控时走近Player1(≤3.5m) → F → "要我继续吗？"确认框 → [好的] → 交换位置 + 切回
- `switch_to_me()` 包含 global_position 互换
- 新增 `_show_switch_back_dialogue()` + `_on_switch_back_yes()`

### Player2 被控时的交互系统
- `_update_interactables()` 每帧扫描 Interactable 组 + 检测 Player1 距离
- 动态设置 `item._player = self` 解决缓存问题
- F键优先检测 Player1（换回）> Interactable（对话/拾取）
- 射线互相排除另一个角色避免红X

### 相机：共享模式
- Player2 通过 `../Player1/CameraPivot` 引用共享相机
- set_controlled 不操作 current，相机始终激活

### 场景结构最终状态
| 角色 | 类型 | 模型 | 脚本 | 功能 |
|------|------|------|------|------|
| Player1 | CharacterBody3D | Walking2.fbx ×1.8 | Player.gd | 主角 |
| Player2 | CharacterBody3D | Walking_p2.fbx ×2 | Player2.gd | NPC/可操控 |
| 测试NPC | StaticBody3D | Walking2.fbx ×1.8 | Npc.gd | 对话NPC |
