# 2026-06-16

## 严重问题修复：恢复 BasePlayer.gd + 墙壁透明双重初始化

### 修复内容
1. **创建 BasePlayer.gd（425行）**，Player.gd 和 Player2.gd 均继承自此基类
   - 提取了 24 个 100% 相同的方法到基类
   - 提取了全部 @export 变量（11个路径导出）
   - 提取了共享数据变量（技能、背包、日志、移动状态等）
   - 提取了共享 @onready UI 引用（10个）
   - `add_item` 通过虚拟方法 `_get_item_log_prefix()` 实现子类差异化日志前缀

2. **修复墙壁透明双重初始化**
   - `_init_wall_transparency` 使用 `walls.set_meta("_transparency_initialized", true)` 防止重复初始化
   - 两个 Player 都调用该方法但只有先执行的会生效

3. **_camera 变量处理**
   - BasePlayer 声明 `var _camera: Camera3D`，子类在 _ready() 中赋值
   - 避免子类 @onready 重声明冲突

### 行数变化
- Player.gd: 698 → 342 行（-356）
- Player2.gd: 1108 → 727 行（-381）
- BasePlayer.gd: 425 行（新增）
- 净减少 ~312 行重复代码
